accelerate==1.8.0
datasets==3.2.0
fire==0.7.0
transformers==4.54.0
torch==2.6.0
# torchvision MUST match torch (transformers' image_utils imports torchvision.ops.nms;
# Colab's preinstalled torchvision is built for a different torch, hence the
# `RuntimeError: operator torchvision::nms does not exist` when versions diverge).
torchvision==0.21.0
pillow==11.1.0
# Colab often installs NumPy 2.4+, which removes private APIs (e.g. _no_nep50_warning)
# that older pandas/pyarrow wheels still expect when datasets loads Parquet. Cap below 2.4
# until the whole stack (pandas, pyarrow, datasets) ships wheels built against NumPy 2.4.
numpy>=1.26.4,<2.4
pandas>=2.2.0
pyarrow>=15.0.0
deepspeed
Levenshtein
tqdm
wandb
pyparsing
zss
peft>=0.13.0
smashcima
musicdiff @ git+ssh://git@github.com/guang-yng/efficient-musicdiff.git